package strategies;

import entities.Entity;

/**
 * Controls the alien's AI movement patterns.
 */
public class AlienMovementStrategy implements MovementStrategy {

    /**
     * Moves the alien entity.
     *
     * @param entity The alien entity to move.
     */
    @Override
    public void move(Entity entity) {
        // Method body intentionally left empty
    }
}
