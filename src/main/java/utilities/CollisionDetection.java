package utilities;

import entities.Entity;
import java.awt.Rectangle;

/**
 * Provides utility methods for collision detection logic.
 */
public class CollisionDetection {

    /**
     * Checks if two entities are colliding.
     *
     * @param a The first entity.
     * @param b The second entity.
     * @return True if a collision is detected, false otherwise.
     */
    public static boolean checkCollision(Entity a, Entity b) {
        return false; // Placeholder return
    }

    /**
     * Gets the bounding rectangle of an entity for collision detection.
     *
     * @param entity The entity.
     * @return The bounding rectangle.
     */
    public static Rectangle getBounds(Entity entity) {
        return null; // Placeholder return
    }
}
